// Set this to the HTTPS API origin before a production mobile build.
// Example: window.NOCTURNE_API_BASE = "https://api.example.com";
window.NOCTURNE_API_BASE = "";
// Leave empty to use the same-origin /api/locale endpoint, or point this at
// the remote API origin for a Capacitor build. The trusted deployment edge
// may provide CF-IPCountry, X-Vercel-IP-Country or X-Country-Code.
window.NOCTURNE_LOCALE_ENDPOINT = "";
